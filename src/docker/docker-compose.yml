services:
  db:
    image: postgres:16-alpine
    container_name: helloworld-db
    restart: always
    environment:
      - POSTGRES_USER=api
      - POSTGRES_PASSWORD=api
      - POSTGRES_DB=test
    ports:
      - "54339:5432"
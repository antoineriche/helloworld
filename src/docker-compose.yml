version: '2'
services:
  test-postgresql:
    container_name: api-test-database
    build: docker
    ports:
      - 54336:5432
    environment:
      - POSTGRES_USER=api
      - POSTGRES_PASSWORD=api
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_DB=mydb
